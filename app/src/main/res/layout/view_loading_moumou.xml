<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center" 
        >

        <ProgressBar
            android:id="@+id/loding_moumou"
            android:layout_width="@dimen/dialog_view_loading_width"
            android:layout_height="@dimen/dialog_view_loading_height"
            android:indeterminateDrawable="@drawable/loading_moumou" />

        <TextView
            android:id="@+id/tv_loding_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/loding_moumou"
            android:layout_marginTop="10dip"
            android:gravity="center_horizontal"
            android:text="@string/wait_min"
            android:textSize="@dimen/dialog_view_loading_text_size"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_loding_text2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_loding_text"
            android:layout_marginTop="10dip"
            android:gravity="center_horizontal"
            android:textSize="18sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </LinearLayout>

</RelativeLayout>